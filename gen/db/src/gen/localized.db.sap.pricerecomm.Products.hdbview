-- generated by cds-compiler version 2.4.4
VIEW localized_db_sap_pricerecomm_Products AS SELECT
  L.ID,
  L.label,
  L.category_ID,
  L.price_ID,
  L.country_code
FROM db_sap_pricerecomm_Products AS L
WITH ASSOCIATIONS (
  MANY TO ONE JOIN localized_db_sap_pricerecomm_Categories AS category ON (category.ID = category_ID),
  MANY TO ONE JOIN db_sap_pricerecomm_Prices AS price ON (price.ID = price_ID),
  MANY TO ONE JOIN localized_sap_common_Countries AS country ON (country.code = country_code)
)