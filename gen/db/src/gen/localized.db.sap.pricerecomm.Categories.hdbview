-- generated by cds-compiler version 2.4.4
VIEW localized_db_sap_pricerecomm_Categories AS SELECT
  L.ID,
  L.name
FROM db_sap_pricerecomm_Categories AS L
WITH ASSOCIATIONS (
  MANY TO MANY JOIN localized_db_sap_pricerecomm_Products AS products ON (products.category_ID = ID)
)